# Config files (keep samples)
config/*.json
!config/*.sample.json
!config/humanizer-rules.json

# Data files (keep templates)
data/connections.csv
data/outreach-history.json
data/tracking.json
data/connection-matches.json
data/company-sizes.json
data/applied-jobs.json
data/selected-jobs.json
data/sent-requests.json
data/resume.*
data/jobs/*.json
!data/jobs/sample-job.json

# Dashboard generated data
dashboard/data.js

# Outputs
outputs/

# Python
__pycache__/
*.pyc

# Environment
.env
venv/

# Auto-generated
CLAUDE.md

# Claude Code per-user local settings (per-machine, should not be shared)
.claude/settings.local.json

# Workspace path file (global install: points to the user's data directory)
.workspace

# RLHF feedback patterns (user-specific, generated by /job-search analyze feedback)
data/feedback-patterns.json
